# Native Messaging host

Run `scripts/build.sh`, then `scripts/install-native-host.sh` from the project root.
The installer writes the Firefox host manifest under
`~/.mozilla/native-messaging-hosts/` and binds it to the fixed extension ID
`biliwebandroidstream@example.invalid`.
